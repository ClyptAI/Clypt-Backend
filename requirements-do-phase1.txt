--extra-index-url https://download.pytorch.org/whl/cu124
-r requirements.txt
torch==2.6.0+cu124
torchvision==0.21.0+cu124
torchaudio==2.6.0+cu124
opencv-python-headless>=4.12.0
rfdetr[onnx]>=1.3.0
supervision>=0.25.0
trackers>=2.1.0
funasr>=1.2.6
setuptools==69.5.1  # tensorflow-hub 0.16.x uses pkg_resources; setuptools>=70 removed it
tensorflow>=2.16.0
tensorflow-hub>=0.16.0
# HF VibeVoice is optional and should be brought up separately; do not force
# flash-attn / liger-kernel into the main worker venv by default.
# The default production path is native VibeVoice in its own venv.
transformers>=5.3.0
nemo-toolkit[asr]
httpx>=0.27.0
