--extra-index-url https://download.pytorch.org/whl/cu124

# Phase 1 AUDIO runtime deps for the RTX 6000 Ada droplet.
#
# This venv runs on the RTX 6000 Ada host and powers:
#   * the FastAPI service at backend.runtime.phase1_audio_service.app:create_app()
#   * the in-process VibeVoice vLLM client (HTTP, no GPU load in this venv)
#   * the ffmpeg-NVENC node-clip extractor used by POST /tasks/node-media-prep
#
# NFA / emotion2vec+ / YAMNet moved back to the H200 — see docs/ERROR_LOG.md
# 2026-04-17 — so we do NOT pull nemo-toolkit / funasr / tensorflow(-hub) /
# librosa / resampy here. RF-DETR and trackers also stay on the H200
# (see requirements-do-phase1-visual.txt).

pydantic>=2.11.0
pytest>=8.4.0
httpx>=0.28.0
python-dotenv>=1.0.1
google-cloud-storage>=2.19.0
fastapi>=0.116.0
uvicorn>=0.35.0
