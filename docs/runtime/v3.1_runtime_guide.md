# V3.1 Runtime Guide

**Status:** Active backend runtime reference for the serial worker branch  
**Branch:** `v3.1-refactor-serial`

This document explains the current live runtime/provider surface for the serial V3.1 worker. It is the source of truth for the setup we are carrying forward before the planned vLLM work.

## Current Working Setup

The currently working setup on this branch is:

- Phase 1 runs **serially** on one GPU
- visual uses **RF-DETR Small + ByteTrack**
- visual default backend is `pytorch_cuda_fp16`
- TensorRT is implemented as the optimized visual path, but is not the default
- ASR uses **native VibeVoice** in a **second venv**
- native VibeVoice runs with:
  - `microsoft/VibeVoice-ASR`
  - `flash_attention_2`
  - Liger enabled
- the main worker venv is pinned to CUDA 12.4 PyTorch wheels so `torchaudio.info(...)` works reliably before the native subprocess is launched
- the worker writes **live per-job logs** and also streams native-worker stderr into those logs

### What is validated right now

Validated on the serial branch:

- native VibeVoice-only runs succeeded on:
  - `60s` Joe Rogan slice
  - `300s` Joe Rogan slice
  - `540s` Joe Rogan slice
  - full `392.9s` MrBeast clip

Important caveat:

- a **fresh full end-to-end serial Phase 1 rerun** on the full `788.7s` Joe Rogan clip has not yet been re-run to completion after the latest UTF-8 subprocess and logging fixes. The next runtime milestone is to do that before switching Phase 1 ASR to vLLM.

## What Exists Now

### Provider config

- [backend/providers/config.py](/Users/rithvik/Clypt-V3/backend/providers/config.py)

This loads:

- VibeVoice ASR settings (replaces Pyannote cloud)
- Vertex AI generation settings
- Vertex AI embedding settings
- GCS bucket settings
- Phase 1 working-directory/runtime defaults

Current required env:

- `GOOGLE_CLOUD_PROJECT` or an active `gcloud` project
- `GCS_BUCKET`
- `GOOGLE_APPLICATION_CREDENTIALS` — path to a GCP service account key JSON; required on headless servers for GCS uploads (service account must have `roles/storage.admin`)

Optional env:

- `VIBEVOICE_BACKEND` (default: `native`) — `native`: subprocess using `requirements-vibevoice-native.txt` venv + `microsoft/VibeVoice-ASR` (~7B, Gradio parity). `hf`: in-process Hugging Face `microsoft/VibeVoice-ASR-HF` (~9B) in the main venv (needs `transformers>=5.3.0`).
- `VIBEVOICE_NATIVE_VENV_PYTHON` — path to the **second** venv’s `python` executable; **required** when `VIBEVOICE_BACKEND=native`
- `VIBEVOICE_MODEL_ID` — default `microsoft/VibeVoice-ASR` (native) or `microsoft/VibeVoice-ASR-HF` (when `VIBEVOICE_BACKEND=hf`)
- `VIBEVOICE_FLASH_ATTN` (default: `1`) — Flash Attention 2 where supported (falls back to SDPA if unavailable)
- `VIBEVOICE_LIGER` (default: `1`) — Liger kernel patches for Qwen2 layers (RoPE, RMSNorm, SwiGLU)
- `VIBEVOICE_MAX_NEW_TOKENS` (default: `32768`)
- `VIBEVOICE_DO_SAMPLE`, `VIBEVOICE_TEMPERATURE`, `VIBEVOICE_TOP_P`, `VIBEVOICE_REPETITION_PENALTY`, `VIBEVOICE_NUM_BEAMS`, `VIBEVOICE_ATTN_IMPLEMENTATION` — generation knobs passed to the native worker or HF path. The currently validated native GPU setting is `VIBEVOICE_ATTN_IMPLEMENTATION=flash_attention_2`
- `VIBEVOICE_SUBPROCESS_TIMEOUT_S` (default: `7200`) — wall-clock limit for the native child process
- `VIBEVOICE_HOTWORDS_CONTEXT` — comma-separated hotwords/context string injected into every VibeVoice request (Gradio-style list); improves pronoun/conjunction recognition
- `VIBEVOICE_SYSTEM_PROMPT` — **HF backend only**: optional override for the built-in diarization-focused system prompt
- `GOOGLE_CLOUD_LOCATION`
- `VERTEX_GEMINI_LOCATION`
- `VERTEX_EMBEDDING_LOCATION`
- `VERTEX_GEMINI_MODEL`
- `VERTEX_EMBEDDING_MODEL`
- `CLYPT_PHASE1_WORK_ROOT`
- `CLYPT_PHASE1_KEEP_WORKDIR`
- `CLYPT_PHASE1_YAMNET_DEVICE`

### YouTube provider env

- `CLYPT_YOUTUBE_COOKIES_FILE` — path to a Netscape-format cookies.txt file; required on headless servers for bot-detection bypass
- `CLYPT_YOUTUBE_JS_RUNTIMES` — JS runtime for yt-dlp's EJS n-challenge solver (e.g. `bun:/root/.bun/bin/bun`); required alongside cookies — without this, yt-dlp fails with "Requested format is not available" even when cookies are valid. **Use `bun` not `node`** — yt-dlp only bundles EJS solver scripts for bun and deno, not node

### Visual pipeline env

- `CLYPT_PHASE1_VISUAL_BACKEND` — detector inference backend (default: `pytorch_cuda_fp16`, alt: `tensorrt_fp16`)
- `CLYPT_PHASE1_VISUAL_BATCH_SIZE` — frames per detector batch (default: `4`)
- `CLYPT_PHASE1_VISUAL_THRESHOLD` — detection confidence threshold (default: `0.35`)
- `CLYPT_PHASE1_VISUAL_SHAPE` — detector input resolution (default: `640`); **must be 640** — RF-DETR's backbone requires inputs divisible by 32 and uses patch size 16; `560` triggers an AssertionError at runtime
- `CLYPT_PHASE1_VISUAL_TRACKER` — tracker backend (default: `bytetrack`)
- `CLYPT_PHASE1_VISUAL_TRACKER_BUFFER` — lost track buffer frames (default: `30`)
- `CLYPT_PHASE1_VISUAL_TRACKER_MATCH_THRESH` — ByteTrack `track_activation_threshold` (default: `0.7`); this is ByteTrack's upstream default — do not raise it without testing, as it delays track activation
- `CLYPT_PHASE1_VISUAL_DECODE` — frame decode backend (default: `cpu`)
- `CLYPT_PHASE1_VISUAL_TRT_ENGINE_DIR` — directory for cached TensorRT engines (default: `backend/outputs/tensorrt_engines`)

## Live provider clients

### VibeVoice ASR

- [backend/providers/vibevoice.py](/Users/rithvik/Clypt-V3/backend/providers/vibevoice.py)
- Native worker (second venv only): [backend/runtime/vibevoice_native_worker.py](/Users/rithvik/Clypt-V3/backend/runtime/vibevoice_native_worker.py)

Implements diarization + transcription in a single pass, replacing Pyannote cloud.

**Default (`VIBEVOICE_BACKEND=native`):** spawns `VIBEVOICE_NATIVE_VENV_PYTHON -m backend.runtime.vibevoice_native_worker` with a JSON job on stdin. That interpreter loads `microsoft/VibeVoice-ASR` (~7B) via the official `vibevoice` package — same dependency line as Microsoft’s Gradio demo, isolated from the main worker’s `transformers>=5` (RF-DETR).

**Optional (`VIBEVOICE_BACKEND=hf`):** in-process Hugging Face `microsoft/VibeVoice-ASR-HF` (~9B) using `VibeVoiceAsrForConditionalGeneration`; uses the **same** primary venv as the rest of Phase 1.

Features:

- `run(audio_path, context_info=None) -> list[dict]` — returns `[{Start, End, Speaker, Content}, ...]`
- Greedy decoding defaults (`do_sample=False`, `num_beams=1`, etc.) unless overridden via env
- HF path: optional custom diarization-focused system prompt via `VIBEVOICE_SYSTEM_PROMPT`
- `context_info` / `VIBEVOICE_HOTWORDS_CONTEXT` — hotwords/context string (comma-separated in `.env`) injected into the request
- Supports long audio; multilingual
- Speaker IDs are integers (0, 1, 2...) — normalized to `SPEAKER_0`, `SPEAKER_1` by `vibevoice_merge.py`
- Audio duration / RTF logging uses `torchaudio.info(...)` only. We intentionally removed the `wave` fallback after it reported bogus `2**30 - 1` frame counts on `joeroganxflagrant.wav` and logged the clip as ~6.8 hours instead of ~13.1 minutes
- HF fallback path downmixes stereo input to mono before inference; this avoids `Audio should be mono` errors on dual-channel WAVs such as `joeroganxflagrant.wav`

**Model download:** first run pulls weights from Hugging Face into `~/.cache/huggingface/` (size depends on 7B vs 9B model).

### Native GPU recipe that actually worked

The now-validated configuration on DigitalOcean is:

- GPU: H200 (`gpu-h200x1-141gb`)
- main worker venv: CUDA 12.4 PyTorch stack from `requirements-do-phase1.txt`
- native ASR venv: CUDA 12.4 PyTorch wheel + `requirements-vibevoice-native.txt`
- `VIBEVOICE_BACKEND=native`
- `VIBEVOICE_NATIVE_VENV_PYTHON=/opt/clypt-phase1/.venv-vibevoice-native/bin/python`
- `VIBEVOICE_ATTN_IMPLEMENTATION=flash_attention_2`
- systemd worker env includes:
  - `HOME=/opt/clypt-phase1`
  - `PYTORCH_KERNEL_CACHE_PATH=/opt/clypt-phase1/.cache/torch/kernels`
- deploy validates `torchaudio.info(...)` in the main worker env before the services are restarted

Important operational note: the prebuilt `flash-attn` wheel was ABI-incompatible with the droplet’s `torch 2.6.0+cu124`. The working fix was to install CUDA toolkit 12.4 on the host and rebuild `flash-attn==2.8.3` from source inside the native venv. Until that rebuild succeeds, the safe fallback is `VIBEVOICE_ATTN_IMPLEMENTATION=sdpa`.

### Forced alignment (NeMo Forced Aligner)

- [backend/providers/forced_aligner.py](/Users/rithvik/Clypt-V3/backend/providers/forced_aligner.py)

Adds per-word timestamps to VibeVoice's turn-level output using NVIDIA NeMo Forced Aligner (NFA).

- `run(audio_path, turns) -> list[dict]` — returns `[{word_id, text, start_ms, end_ms, speaker_id}, ...]`
- Slices each turn's audio segment, runs Viterbi CTC forced alignment, returns absolute word timestamps
- Uses `stt_en_fastconformer_hybrid_large_pc` (FastConformer hybrid model in CTC mode, ~1GB, auto-downloaded from NGC on first run)
- Degrades gracefully: if `nemo_toolkit[asr]` is not installed, logs a warning and returns empty `[]` — the rest of the pipeline uses token-split fallback timing in `vibevoice_merge.py`

### Vertex AI

- [backend/providers/vertex.py](/Users/rithvik/Clypt-V3/backend/providers/vertex.py)

Implemented:

- `VertexGeminiClient.generate_json(...)`
- `VertexEmbeddingClient.embed_texts(...)`

Current expectation:

- Vertex-backed Gemini generation using Gemini 3.1 Pro
- Vertex-backed embeddings using Gemini Embedding 2
- separate generation and embedding locations where needed

### Storage

- [backend/providers/storage.py](/Users/rithvik/Clypt-V3/backend/providers/storage.py)

Implemented:

- GCS upload of local files to `gs://...`
- `get_https_url(gcs_uri, expiry_hours)` — generates a V4 signed HTTPS URL for a GCS object; requires `GOOGLE_APPLICATION_CREDENTIALS` to point to a service account key with `roles/storage.admin` and `roles/iam.serviceAccountTokenCreator`

**Note:** Our GCS bucket uses uniform bucket-level access. The `blob.make_public()` fallback will fail with `400 BadRequest`. Signed URLs via service account key are the only working path.

### Local audio-model providers

- [backend/providers/vibevoice.py](/Users/rithvik/Clypt-V3/backend/providers/vibevoice.py) — VibeVoice-ASR (diarization + transcription)
- [backend/providers/forced_aligner.py](/Users/rithvik/Clypt-V3/backend/providers/forced_aligner.py) — NeMo Forced Aligner (word-level timestamps)
- [backend/providers/emotion2vec.py](/Users/rithvik/Clypt-V3/backend/providers/emotion2vec.py)
- [backend/providers/yamnet.py](/Users/rithvik/Clypt-V3/backend/providers/yamnet.py)

Implemented:

- VibeVoice-ASR: diarization + transcription (native ~7B subprocess or HF ~9B in-process); takes audio path, returns turns with speaker IDs and timestamps
- NeMo Forced Aligner: adds word-level timestamps to VibeVoice turns; requires `nemo-toolkit[asr]`
- `emotion2vec+` turn-level orchestration (clips each turn, runs model, normalizes to Phase 1 shape)
- YAMNet event merging provider (normalizes raw/patch event output into merged event spans)

Important:

- `emotion2vec+` requires `funasr` **and** `torchaudio>=2.0.0`
- NeMo Forced Aligner requires `nemo-toolkit[asr]` and `torchaudio>=2.0.0`
- HF VibeVoice backend requires `transformers>=5.3.0` in the main venv; the **native** backend uses a separate venv (`requirements-vibevoice-native.txt`) so RF-DETR can stay on `transformers>=5` without conflicting with `vibevoice`’s pinned stack
- native VibeVoice on GPU is now validated on H200 with `flash_attention_2`; if `flash-attn` fails to import on a fresh host, rebuild it against the exact CUDA / PyTorch pair or temporarily set `VIBEVOICE_ATTN_IMPLEMENTATION=sdpa`
- the default live `YAMNet` path uses TensorFlow + TensorFlow Hub
- the intended worker default is `CLYPT_PHASE1_YAMNET_DEVICE=gpu`
- all audio providers are wired into the V3.1 Phase 1 worker/runtime surface

## Phase 1 runtime

### Workspace model

- [backend/phase1_runtime/models.py](/Users/rithvik/Clypt-V3/backend/phase1_runtime/models.py)

Key types:

- `Phase1Workspace`
- `Phase1SidecarOutputs`

### Sidecar orchestration

- [backend/phase1_runtime/extract.py](/Users/rithvik/Clypt-V3/backend/phase1_runtime/extract.py)

Implemented (all serial, single GPU lane):

1. visual extraction (RF-DETR Small + ByteTrack)
2. VibeVoice-ASR (diarization + transcription)
3. NeMo Forced Aligner (per-turn word-level timestamps)
4. `emotion2vec+` (turn-level emotion scoring)
5. `YAMNet` (scene audio event classification)
6. merge outputs into canonical `Phase1SidecarOutputs`

The `Phase1SidecarOutputs.diarization_payload` field (formerly `pyannote_payload`) contains the merged `{words, turns}` dict from `vibevoice_merge.py`. The `identify_payload` field has been removed — VibeVoice does not support external voiceprint identification.

### Visual extraction

The visual extraction stack has been refactored to use RF-DETR Small + ByteTrack:

- **Detector:** `RFDETRSmall` from the `rfdetr` library
- **Tracker:** `ByteTrackTracker` from Roboflow `trackers`
- **Decode:** CPU-based frame decoding via OpenCV

The old Ultralytics/YOLO-based detector path has been removed.

Architecture:

1. `visual.py` — orchestration: metadata probe, shot detection, detector factory, pipeline coordination, artifact post-processing
2. `rfdetr_detector.py` — PyTorch CUDA RF-DETR model loading, FP16 optimization, person-only detection
3. `tensorrt_detector.py` — ONNX export, TensorRT engine build, native TRT FP16 inference, person-only detection
4. `frame_decode.py` — CPU video frame decoding, batch assembly
5. `tracker_runtime.py` — ByteTrackTracker lifecycle, sequential frame updates, track row emission
6. `visual_config.py` — env-driven configuration for detector/tracker/decode/TRT settings

The detector factory in `visual.py` (`_make_detector`) selects between `RFDETRPersonDetector` (PyTorch CUDA) and `TensorRTDetector` (native TRT) based on `config.use_tensorrt`.

Key properties:

- CUDA is required for detection — no silent CPU fallback
- FP16 inference is the default
- Two detector backends:
  - `pytorch_cuda_fp16` — PyTorch with FP16 and `torch.compile` (default, works on any CUDA GPU)
  - `tensorrt_fp16` — native TensorRT engine with FP16 (fastest, requires TensorRT install)
- TensorRT path auto-exports ONNX and converts to `.engine` on first run, then caches
- Engines are GPU-specific: rebuild when switching GPU families
- detection is batched, tracking is sequential in frame order
- person class only (COCO class **1** — RF-DETR uses class 0 for background; class 1 is person)

Visual extraction emits:

- `video_metadata`
- `shot_changes` (via ffmpeg scene-cut detection)
- `tracks` (shot-local, with normalized geometry and bbox_norm_xywh)
- `person_detections` (derived from shot-split tracks)
- `tracking_metrics` (with RF-DETR runtime labels, latency, and throughput)

Current intentional omissions in the visual payload:

- `face_detections`
- `visual_identities`
- `mask_stability_signals`

Those remain empty in the current Phase 1 runtime because the V3.1 Phase 1-4 build does not revive the old face identity / LR-ASD stack.

Additional runtime modules now present:

- [backend/phase1_runtime/media.py](/Users/rithvik/Clypt-V3/backend/phase1_runtime/media.py)
- [backend/phase1_runtime/visual.py](/Users/rithvik/Clypt-V3/backend/phase1_runtime/visual.py)
- [backend/phase1_runtime/rfdetr_detector.py](/Users/rithvik/Clypt-V3/backend/phase1_runtime/rfdetr_detector.py)
- [backend/phase1_runtime/tensorrt_detector.py](/Users/rithvik/Clypt-V3/backend/phase1_runtime/tensorrt_detector.py)
- [backend/phase1_runtime/frame_decode.py](/Users/rithvik/Clypt-V3/backend/phase1_runtime/frame_decode.py)
- [backend/phase1_runtime/tracker_runtime.py](/Users/rithvik/Clypt-V3/backend/phase1_runtime/tracker_runtime.py)
- [backend/phase1_runtime/visual_config.py](/Users/rithvik/Clypt-V3/backend/phase1_runtime/visual_config.py)
- [backend/phase1_runtime/runner.py](/Users/rithvik/Clypt-V3/backend/phase1_runtime/runner.py)
- [backend/phase1_runtime/state_store.py](/Users/rithvik/Clypt-V3/backend/phase1_runtime/state_store.py)
- [backend/phase1_runtime/jobs.py](/Users/rithvik/Clypt-V3/backend/phase1_runtime/jobs.py)
- [backend/phase1_runtime/worker.py](/Users/rithvik/Clypt-V3/backend/phase1_runtime/worker.py)
- [backend/phase1_runtime/app.py](/Users/rithvik/Clypt-V3/backend/phase1_runtime/app.py)
- [backend/phase1_runtime/factory.py](/Users/rithvik/Clypt-V3/backend/phase1_runtime/factory.py)

## Live Phase 1-4 runner

- [backend/runtime/phase14_live.py](/Users/rithvik/Clypt-V3/backend/runtime/phase14_live.py)

Implemented:

- Phase 1 artifact persistence from real sidecars
- Phase 2 live merge/classify calls
- Phase 2 live embedding attachment
- Phase 3 live local semantic edges
- Phase 3 live long-range adjudication
- Phase 4 live subgraph review and pooled final review

This is the first real provider-backed path in the branch.

## Injected vs live execution

There are now two execution styles:

### Injected/testing path

- [backend/pipeline/orchestrator.py](/Users/rithvik/Clypt-V3/backend/pipeline/orchestrator.py)

Use this when:

- testing with prebuilt sidecar payloads
- testing with prebuilt Gemini responses
- verifying adapters and contracts

### Live/provider-backed path

- [backend/runtime/phase14_live.py](/Users/rithvik/Clypt-V3/backend/runtime/phase14_live.py)

Use this when:

- you already have Phase 1 sidecars
- you want real Vertex AI and embedding execution for Phases 2-4

## What Still Does Not Exist Yet

The major remaining runtime gaps are:

- face/identity-oriented visual ledgers beyond person tracking
- broader operational observability beyond SQLite job state + per-job logs
- TensorRT vs PyTorch CUDA FP16 benchmark comparison on the current H200 droplet class

The local CLI now exists:

- [backend/runtime/run_phase1.py](/Users/rithvik/Clypt-V3/backend/runtime/run_phase1.py)

The branch now also includes the Phase 1 service, worker, and remote-client shell under:

- [backend/runtime/run_phase1_service.py](/Users/rithvik/Clypt-V3/backend/runtime/run_phase1_service.py)
- [backend/runtime/run_phase1_worker.py](/Users/rithvik/Clypt-V3/backend/runtime/run_phase1_worker.py)
- [scripts/do_phase1/run_remote_job.py](/Users/rithvik/Clypt-V3/scripts/do_phase1/run_remote_job.py)
- [scripts/do_phase1/run_remote_job.sh](/Users/rithvik/Clypt-V3/scripts/do_phase1/run_remote_job.sh)

Operational notes for the current worker shell:

- API and worker share the same SQLite DB path and log root
- per-job log files are created immediately when the worker claims a job
- job logs now capture:
  - Python `logger.info(...)` output
  - redirected stdout/stderr
  - native VibeVoice subprocess stderr lines

So the largest remaining operational step on this branch is a fresh full serial Phase 1 rerun on the long Joe Rogan clip, not missing runtime code.
